<!DOCTYPE html>
<html lang=“en”>
<head>
    <meta charset=“UTF-8">
    <meta name=“viewport” content=“width=device-width, initial-scale=1.0">
    <title> Library</title>
</head>
<body>
    <h1>Library</h1>
    <ul>
        <nav>
            <a href=“/library/new”>Add New Listing</a>
        </nav>
        <% for( let i = 0; i < library.length; i++ ) { %>
            <h1><%= library[i].name %> </h1>
            <a href="/library/<%= library[i].id %>"><img src=" <%= library[i].img %>  " alt=“”></a> <br>
            Genre: <p><%= library[i].genre %> </p>
            Price: <h3><%= library[i].price %></h3>
            <form action=“/library/<%= library[i].id%>?_method=DELETE” method=“POST”>
                <input type=“submit” value=“DELETE”/>
            </form>
        <% } %>
    </ul>
</body>
</html>